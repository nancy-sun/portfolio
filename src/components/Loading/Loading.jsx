import React from "react";
import "./Loading.scss";

function Loading() {
    return (
        <main className="loading">
            <h1 className="loading__logo">N</h1>
        </main>
    );
};

export default Loading;